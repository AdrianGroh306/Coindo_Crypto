{
  "name": "coindo_crypto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tailwindcss -i src/styles.css -o dist/styles.css && cp src/index.html dist/",
    "copy": "copyfiles -u 1 src/index.html dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss build -i src/input.css -o dist/output.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.6"
  }
}
